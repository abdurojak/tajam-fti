# Copy to .env.local. Empty values preserve local SQLite mode without login.
# Never commit .env.local or real credentials.
DATABASE_URL=
NEXTAUTH_URL=
NEXTAUTH_SECRET=
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
# Optional local SQLite location; ignored when DATABASE_URL is configured.
TAJAM_DB_PATH=
